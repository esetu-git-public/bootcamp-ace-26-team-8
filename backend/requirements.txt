# --------------------------------------------------------------------------- #
# Loan Default Prediction System — Backend (FastAPI) requirements
# --------------------------------------------------------------------------- #
# Version pins for pandas / numpy / scikit-learn / joblib are matched EXACTLY
# to ml/requirements.txt so that joblib.load("loan_pipeline.pkl") deserializes
# the sklearn Pipeline (ColumnTransformer + LogisticRegression) without any
# version-skew warnings or incompatibilities at inference time.
# --------------------------------------------------------------------------- #

# --- Web framework / ASGI server ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Settings & validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
email-validator==2.2.0

# --- Auth (Supabase JWT verification) ---
PyJWT[crypto]==2.9.0

# --- ML pipeline deserialization / inference (pinned to match ml/requirements.txt) ---
joblib==1.4.2
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1

# --- Supabase client ---
supabase==2.7.4

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
requests==2.32.3