# --------------------------------------------------------------------------- #
# Loan Default Prediction System — Frontend environment variables
#
# Copy this file to `.env.local` for local development. Never commit a real
# `.env.local`. In Vercel, set these as Project Environment Variables — note
# that NEXT_PUBLIC_* vars are baked in at BUILD time, not runtime.
# --------------------------------------------------------------------------- #

# Supabase client init (Auth session handling only — anon key is safe to
# expose; it can never bypass the backend's service-role-gated writes).
NEXT_PUBLIC_SUPABASE_URL=https://fqnzcwdnqsgefgsqfujy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZxbnpjd2RucXNnZWZnc3FmdWp5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODM5MjgzOTMsImV4cCI6MjA5OTUwNDM5M30.8XqLf5_7gURVPTg_kk7CCbKYfKRAV2awzU6YMvEJPpA

# Points apiClient.ts at the deployed FastAPI backend.
# Local dev default matches backend/render.yaml's local uvicorn port.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1